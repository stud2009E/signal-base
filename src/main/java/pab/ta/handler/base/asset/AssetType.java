package pab.ta.handler.base.asset;

/**
 * Enumerates the different types of financial assets supported by the system.
 * This enum categorizes assets based on their fundamental financial instrument type.
 */
public enum AssetType {
    SHARE,
    FUTURE,
    CURRENCY,
    INDEX
}
